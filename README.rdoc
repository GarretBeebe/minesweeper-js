== README

To run:

* cd into project directory

* run:

  npm install

* run:

  npm start

* In your browser of choice, visit:

    http://localhost:3000

* A WIP example app is deployed on heroku. It can be found at:

  https://myminesweeperjs.herokuapp.com/
